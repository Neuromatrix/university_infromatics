println("Введите координаты х и у")
x = parse(Float32, readline())
y = parse(Float32, readline())
println("Программа вычисляет, принадлежит ли точка с Вашими координатами графику функции")
println("-----------------------------------")
if ((x+1)^2+y^2<1) && (y>=0.5) && (x>=-1)
    print("Принадлежит")
else 
    print("Не принадлежит")
end
